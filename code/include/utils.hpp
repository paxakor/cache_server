// Copyright 2016, Pavel Korozevtsev.

#pragma once

#include <string>
#include <vector>

namespace pkr {

std::vector<std::string> split(const std::string&, size_t = 0, char = 0);

}  // namespace pkr
